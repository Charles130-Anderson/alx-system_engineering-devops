#!/usr/bin/env bash
#comands For installing nginx
sudo apt-get update
sudo apt-get install nginx
ufw 'Nginx HTTP'
echo "Hello World!" > /var/www/html/index.html
sudo nginx -t
sudo service nginx reload
sudo service nginx restart
