#!/usr/bin/env bash
# commands for installing nginx

sudo apt-get -y update
sudo apt-get install -y nginx
sudo ufw 'Nginx HTTP'
sudo nginx -t

sudo service nginx restart
echo "Hello world!" | sudo tee /var/www/html/index.htmli
